# Feat: Explore vision, scope next iteration, and decompose into work items (L-VIS-17)

**Priority:** Low
**Source:** Self-improvement loop
**Depends on:** H-DX-1, H-DX-2, M-DX-3, M-DX-4, H-NX-2, H-NX-3, H-CA-4, M-TUI-5, M-TM-5
**Domain:** vision

This is a recurring meta-item. When all other work items are complete, this item triggers a new cycle: (1) Review the current state of ninthwave against the product vision — what's shipped, what's missing, what friction was logged. (2) Read the friction log and identify actionable improvements. (3) Identify the next most impactful capability or refinement. (4) Decompose it into work items following the standard format. (5) Add a new copy of this same item (L-VIS-18, etc.) depending on the new terminal items, so the cycle continues.

Acceptance: New work items are written to `.ninthwave/work/`. A new vision exploration item is added depending on the new terminal items. The friction log is reviewed and actionable items are addressed. `.ninthwave/work/` is non-empty after this item completes.

Key files: `.ninthwave/work/`, `CLAUDE.md`, `README.md`, `VISION.md`
