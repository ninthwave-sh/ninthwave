source: supervisor
date: 2026-03-27T13:11:30Z
---
- [friction] Worker hit Claude Max rate limit mid-implementation, churned for ~6 minutes before recovering. Daemon had no visibility into this — elapsed time was the only signal. Screen scraping for "hit your limit" could enable faster detection.
- [friction] Health detector reported stalled-empty 3 times while worker was actively composing/forging (long LLM generation). These are false positives — composing state shows minimal terminal output but the worker is actively generating.
- [friction] PreToolUse:Edit and PostToolUse:Write hook errors appeared on every edit/write but did not block operations. Silent hook failures may mask real issues.
- [improvement] Screen health heuristic should treat "Composing" / "Forging" spinner lines as active states, not stalls.
- [improvement] Orchestrator could detect rate-limit patterns ("hit your limit", "resets Xpm") via screen scraping and pause the item timer rather than counting throttled time against the worker.
- [improvement] Consider prompting workers to commit after each logical unit (implementation, then tests) for better progress signal — this worker went 30 minutes with no commits while building a complete changeset.
