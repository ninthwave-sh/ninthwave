version: '3'

tasks:
  default:
    cmds:
      - task --list

  test:
    desc: Run all tests
    cmds:
      - bun test

  bump:
    desc: Bump version and generate CHANGELOG entry
    cmds:
      - bun run scripts/version-bump.ts
